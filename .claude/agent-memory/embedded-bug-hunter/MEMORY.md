# Embedded Bug Hunter — Memory Index

- [User: Nate / firmware owner](user-nate.md) — owns Galdr firmware; partners with firmware-engineer agent; wants facts not guesses
- [Two bughunters on the lockup investigation](coordination-two-bughunters.md) — blue "bughunter" owns §13 + A/B diagnosis; a 2nd instance must cross-check, not re-edit the shared doc; resolve split via team-lead
- [Streaming lockup investigation](streaming-lockup-doc.md) — pointer to the owned investigation doc + its current frontier
- [Spurious-ack host accounting](skirnir-spurious-ack-accounting.md) — where skirnir's in-flight/ack tolerance lives and the Unresponsive-disconnect zero-tolerance gap
- [Firmware ack/TX path facts](firmware-ack-tx-path.md) — single RESPONSE channel→usb_tx; CRLF collapse rules out framer double-ok; $G is pure core-0
- [Drumbeat = lost USB TX-done waker; FIX INCOMPLETE](firmware-two-2s-timeouts.md) — H-A lost-waker (usb_tx, NOT RMT). Fix SHIPPED on main (6024126) but wedge STILL reproduces: Sig A = write-stage/ISR-never-armed flavor (iena=1) the flush-stage-only fix can't catch (task #18); Sig B = silent hard lock via the WATCHDOG DEAD ZONE (Stage0-only sw-fed RWDT; host-quiet+exec=0 → never resets; B-1=recovered-wakes re-mask COMMS_PROGRESS) (task #19). Doc §13
- [Silent gcode-SKIP = mid-block RMT truncation](silent-gcode-skip-rmt-truncation.md) — USER-reported part corruption (multi-gap, continues, non-det). LEADING root cause (verified): a multi-burst cutting block abandons its tail via emit_burst(&burst)? on a discarded Transport err (motion.rs:786 `let _=`). Host/overflow/reset ALL ruled out. Probe = RUN_BLOCK_TRUNCATED counter. Task #22, doc §15
- [RWDT/SuperWDT hardware-watchdog facts](firmware-rwdt-superwdt-facts.md) — Sig-B crux: RWDT is a hw RTC-slow-clock counter (esp-rtos 0.3.0 NEVER touches it); the SOFTWARE feeder is an embassy async task that can die; esp32s3 HAS a SuperWDT (Swd, unarmed) §13.4 wrongly denied. Paradox: feeder stopped AND hw dog still didn't fire (>60s silent). PLAN: arm Swd as the B-capture instrument. Byte-correction: §17.7 "mid-write truncation" was a stale boot-replay cut at 64B; true fingerprint = status dies +1585s, drumbeat onsets, hard lock. Doc §17.8/§17.9
